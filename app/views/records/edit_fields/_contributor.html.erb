<%=
  f.input key,
  as: :multi_value,
  input_html: {
  class: 'form-control',
  data: { 'autocomplete-url' => "/authorities/search/lcnames",
'autocomplete' => key }
} ,
required: f.object.required?(key) %>

<%= f.input :authorities, collection: f.object.authorities, prompt: "Select an authority", label: false %>


